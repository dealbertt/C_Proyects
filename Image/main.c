#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define IMAGE_NAME "sample.jpg"

//we first work with an array of pixels  that hold the value of the gray scale(not colors yet)
int main(){
    FILE *ptr = fopen(IMAGE_NAME,"r");    

}
